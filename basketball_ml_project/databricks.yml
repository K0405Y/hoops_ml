# This is a Databricks asset bundle definition for basketball_ml_project.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: basketball_ml_project

include:
  - resources/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-8066068091517004.4.azuredatabricks.net

  prod:
    mode: production
    workspace:
      host: https://adb-8066068091517004.4.azuredatabricks.net
      # We explicitly specify /Users/kehinde.awomuti@pwc.com to make sure we only have a single copy.
      root_path: /Users/kehinde.awomuti@pwc.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: kehinde.awomuti@pwc.com
        level: CAN_MANAGE
    run_as:
      user_name: kehinde.awomuti@pwc.com
